package com.library;

import java.util.Map;

public class Library {
	private Map<Integer,Tray>map;
	private BookSearch bookSearch;
    private IssueMap issue;
    
     IssueMapBooks(studentId, bookname) {
        manager.issue();
    }
    returnBooks(studentId, bookname) {
           mamager.return();
        
    }

    addBooks(name, auther){
        add Tray;
        search(Bookid, BookDetails) ;       
    }

}
